import type { AgentRole } from '../types'
export function AgentRolesPanel({ roles }: { roles: AgentRole[] }) { return <section className="panel"><div className="panel-header"><div><p className="eyebrow">Sub-agent visibility</p><h3>Agent roles</h3></div><span className="pill watch">Runtime-aware</span></div><div className="agents-list">{roles.map((role) => <article key={role.id} className="agent-row"><div className="agent-title-row"><div><strong>{role.role}</strong><p>{role.detail}</p></div><span className="agent-status">{role.status}</span></div><div className="meta-grid"><span>{role.surface}</span><span>{role.runtimeState}</span></div><div className="meter-block"><div className="panel-header"><span>Load</span><span>{role.load}%</span></div><div className="meter-track"><div className="meter-fill" style={{ width: `${role.load}%` }} /></div></div></article>)}</div></section> }
